@*Define the model*@
@model IEnumerable<Warranty>
@*Define ViewData*@
@{
    ViewData["Title"] = "List of available warranties.";
}

@*Code Part*@
<div class="row">
    <div class="col-md-8 offset-md-2">
        <table class="table">
            <thead>
                <tr class="text-center">
                    <th>@Html.DisplayNameFor(model => model.warrantyPictureURL)</th>
                    <th>@Html.DisplayNameFor(model => model.warrantyName)</th>
                    <th>@Html.DisplayNameFor(model => model.warrantyDescription)</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="align-middle">
                            <img class="img-thumbnail" src="@item.warrantyPictureURL" alt="@item.warrantyName" style="max-width: 160px" />
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(model => item.warrantyName)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(model => item.warrantyDescription)
                        </td>
                        <td class="align-middle">
                            <a class="btn btn-outline-primary" asp-action="Details" asp-route-id="@item.Id"><i class="bi bi-bar-chart-fill" style="color:red"></i>Details </a> -
                            <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i>Edit </a> -
                            <a class="btn btn-danger text-white-50" asp-action="Delete" asp-route-id="@item.Id"><i class="bi bi-trash"></i>Delete </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@await Html.PartialAsync("CreatePartial", "Warranty")
